---
import PageLayout from '../../../layouts/PageLayout.astro';
import Breadcrumb from '../../../components/Breadcrumb.astro';
import BookingCta from '../../../components/BookingCta.astro';
import { getCollection } from 'astro:content';

const lang = 'ru';

const posts = (await getCollection('blog', ({ data }) => data.lang === 'ru'))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---

<PageLayout
  lang={lang}
  title="Блог — Анастасия Никольская"
  description="Статьи о тренировках во время беременности и после родов в Самаре."
  canonicalPath="/ru/blog/"
  alternatePath="/en/blog/"
>
  <Breadcrumb lang={lang} items={[{ label: 'Блог' }]} />

  <section class="bg-white">
    <div class="container section">
      <p class="label-uppercase mb-4">Блог</p>
      <h1>Статьи</h1>

      {posts.length === 0 ? (
        <p class="mt-8 text-[var(--color-text-body)]">Статьи скоро появятся.</p>
      ) : (
        <div class="mt-10 grid gap-8 sm:grid-cols-2">
          {posts.map((post) => (
            <a href={`/ru/blog/${post.slug.replace('ru/', '')}/`} class="card block hover:shadow-md transition-shadow no-underline group">
              <p class="text-xs text-[var(--color-text-body)] mb-2">
                {post.data.pubDate.toLocaleDateString('ru-RU', { year: 'numeric', month: 'long', day: 'numeric' })}
              </p>
              <h2 class="text-lg font-bold text-[var(--color-dark)] group-hover:text-[var(--color-primary)] transition-colors mb-2">
                {post.data.title}
              </h2>
              <p class="text-sm text-[var(--color-text-body)]">{post.data.description}</p>
            </a>
          ))}
        </div>
      )}
    </div>
  </section>

  <section class="bg-[var(--color-bg-cream)]">
    <div class="container py-12 text-center">
      <BookingCta lang={lang} label="Записаться на стартовую встречу" />
    </div>
  </section>
</PageLayout>
